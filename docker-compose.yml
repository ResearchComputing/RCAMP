version: "3.6"

services:
  rcamp-uwsgi:
    build: rcamp
    volumes:
      - static-content:/home/uwsgi/rcamp/static
      - media-uploads:/home/uwsgi/rcamp/media

volumes:
  static-content:
  media-uploads:
