version: "3.6"

services:
  rcamp-uwsgi:
    build: rcamp
    links:
      - nginx
      - test-ldap

  test-ldap:
    image: researchcomputing/rc-test-ldap

  nginx:
    images: nginx:stable
    ports:
      - "80:80"
      - "443:443"
